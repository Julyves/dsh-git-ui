/**
 * Git status widget — client half placeholder.
 * The full plugin body (locale, Remote mount, slots injection) lands in the
 * phase-2 implementation; this stub validates the ModuleLoader closure format.
 */

window.__ModuleLoader__.load({
  id: 'dsh-git-status',
  factory: () => {
    // Placeholder: no UI contribution yet.
  },
})
